# ─── Wallet ────────────────────────────────────────────
# Your EOA private key (with or without 0x prefix)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# ─── RPC ───────────────────────────────────────────────
# Alchemy / Infura RPC URLs for each chain
ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/YOUR_KEY
POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY

# ─── LI.FI ─────────────────────────────────────────────
# Optional: LI.FI API key for higher rate limits
LIFI_API_KEY=

# ─── Yellow Network ───────────────────────────────────
# Clearnet endpoint (sandbox for testing)
YELLOW_WS_URL=wss://clearnet-sandbox.yellow.com/ws
YELLOW_FAUCET_URL=https://clearnet-sandbox.yellow.com/faucet/requestTokens

# ─── Prediction Config ─────────────────────────────────
# Asset to predict on
PREDICTION_ASSET=ETHUSD
# Direction: LONG or SHORT
PREDICTION_DIRECTION=LONG
# Amount in USDC (6 decimals) — e.g. 50 USDC = 50000000
PREDICTION_AMOUNT=50000000
# Target price (e.g. 3300.00 for ETH)
PREDICTION_TARGET_PRICE=3300.00
# Expiry in seconds from now (e.g. 3600 = 1 hour)
PREDICTION_EXPIRY_SECONDS=3600
